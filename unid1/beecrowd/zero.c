#include <stdio.h>
#include <string.h>

#define TAM 1000010

void converter_int_string(char* string, int num);

int main(void){
  int a;
  int b;
  
  scanf("%d %d", &a, &b);
  
  char resultado[TAM];
  
  for(int i = 0; i < )
  
}

void converter_int_string(char* string, int resultado){
  snprintf(string, TAM, "%d", resultado);
}
